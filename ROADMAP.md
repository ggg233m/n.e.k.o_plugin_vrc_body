# 路线图

本文件的作用是**防止方向漂移**。改动方向前先读「明确不做」和「不可违反的约束」
两节；要往里加东西，先确认它服务于北极星，而不是只是「看起来该做」。

## 北极星

**让一个通用 agent 能流畅地玩 VRChat。**

验收标准就是字面意思：把任意LLM agent接上这套身体，它能在私人房里
站着聊天、跟着人走、注意到有人挥手并回应、被搭话时转过去。做到这个程度，换别的
agent 也不会有结构性障碍——所以不为某一个模型做特化。

「流畅」的边界见「明确不做」第 2 条：这条路径覆盖的是**社交向**玩法，不是实时反射
类玩法。

## 能力阶梯（截至 2026-08-21）

| # | 能力 | 状态 | 说明 |
|---|------|------|------|
| 1 | 在场 / idle 自然 | ✅ | VMC 待机中转 + VMD 语义片段 + `body_express` |
| 2 | 文字交流 | ✅ | `body_chatbox`。**无语音**——全仓库没有音频通路 |
| 3 | 姿态 / 手势 | ✅ | 调度器 120 Hz，安全子集比驱动本身更严 |
| 4 | 走向可见目标 | ⚠️ | 10 Hz 闭环 + 内置 Velocity 卡墙判据；**仍无障碍感知**，只能撞上之后察觉，且参数名未经真机验证 |
| 5 | 手部交互 | ⚠️ | 能发握持输入，`object_held` 永远 unknown，不能确认拿到 |
| 6 | 看懂场面 | ⚠️ | 已能看到像素（`vrc_vision_frame` + 唤醒配图）；检测器本身仍无深度/OCR；**已知会把墙上立绘检成人**（见「明确不做」第 1b 条） |
| 7 | 社交反应 | ⚠️ | 能被「有人靠近 / 有人挥手 / 近处的人离开」叫醒；挥手仍依赖检测器发事件，本地检测器目前不发 |

### 第 6 行原本为什么是缺口

`tool_defs.py` 里 30+ 个工具，曾经没有一个返回像素。`world_observe` 给 agent 的是压平后
的结构化快照（label / bbox / confidence / bearing_deg / apparent_height），连 VLM 的理解
也在后端就被压成 entity/event 才送出去。

这对 10 Hz 反射式导航循环是**正确设计**（它要的就是有界标量），但对一个要「玩」的
agent 等于蒙眼递坐标表：知道「-12° 有个 person，画面高度占比 0.4」，不知道对方在挥手、
在指什么、自己是否卡在墙角、房间在办活动还是闲聊。

P0 补上的是这条并行通道——`vrc_vision_frame` 与唤醒配图让 agent 看得到像素，而结构化
快照原样保留给导航器。两者**不合流**：像素只喂理解，`world_state` 仍然只由检测器写入。

## 下一步（有序）

### P0 — 让 agent 能看见

- [x] `vrc_vision_frame` 工具：`VisionRuntime` 单槽帧缓存（`process_frame` 里、
      `_release_frame` 之前编码）→ `GET /vision/frame` → `RemoteVision.frame` → 工具。
      图不能走工具返回值（`Ok` 只是 JSON，模型看不到里面的 base64），所以用
      `push_message` 的 image part 让宿主注入当前回合
- [x] 唤醒推送带图：被判定为社交相关的 delta 走 `respond` 时附一张 ≤2 s 的画面；
      `read` 推送不配图——宿主对 read 的图是立刻注入的，配上 2 Hz 节奏就是刷屏
- [x] 限流：`frame_max_per_minute`（默认 10，滑动窗口，见 `frame_budget.py`）。
      没用令牌桶——桶允许攒额度，安静十分钟后能一口气连拉十张，恰好是最该拦的突发
- [x] `instructions.py` 增加对应约束条款（第 23、24 条）
- [ ] 可选叠加检测框，用于对照「检测器看到的」与「画面里实际有的」
- [ ] 取到的帧顺带落盘，自然积累回归语料
- [ ] **用真机会话验证这条路真的通**——单元测试只覆盖到 `push_message` 之前；
      宿主 `stream_image` 那一段（会话存在与否、图过大时的重压/丢弃）没有被测到，
      `frame_submitted=true` 只代表已提交给宿主

排在 P4 前面的理由：cognition 那个规划器接缝假设下面已经有可信的世界模型，而现在
没有。而 agent 一旦能看图，它自己就变成了排查其余各项的仪器。

### P1 — 用真机会话验证感知

- [x] **确认 27×27 px 假阳性是在哪个坐标系量的**——问题不成立：预处理是无 letterbox
      的直接拉伸（`local_perception.py` 的 `cv2.resize`），归一化按轴各自进行，所以
      模型空间和采集区域的**归一化每轴比例是同一个数**。但由此得到的推论要记牢：
      标定必须分别记录宽比和高比，一个数不够
- [x] 据此拆分 `min_box_ratio`：改为按轴设阈值。共用 2% 时总是宽度先卡（1920 下最小
      宽 38 px），按人体长宽比反推，能进入世界的最小 `apparent_height` 有 7%~11%，
      而导航器目标是 0.55——房间对面的人被静默丢掉。现默认宽 0.8% / 高 2%
- [ ] **用真机会话校准这两个默认值**——它们仍是按几何反推的估计，不是实测；单元测试
      全绿不能说明问题
- [ ] 攒真实帧回归集：正常 2~3 人 / 带镜子 / avatar 与场景物件重叠 / 空房
- [ ] 量化镜面倒影与合并框的发生率（**只测不修**，理由见「明确不做」第 1 条）

### P2 — 让身体知道自己有没有动（第 4 行）

在此之前，全仓库没有任何一条通路能回答「我真的移动了吗」：AnyaDance 与 OSC 的
`accepted=true` 都只代表**本机 UDP 发送成功**，UDP 也不会回执。于是导航器分不清
「正在前进」和「顶着墙推摇杆」，一头撞进墙里可以永远推下去。

数据其实一直在：`_handle_message` 缓存了收到的每一个 `/avatar/parameters/*`，只是
被 `awareness_parameters` 白名单挡在 awareness 之外。所以这不是新链路，是打开一扇
已经存在的门，再从中导出一个信号。

- [x] 白名单加入六个 VRChat 内置 Avatar 参数（`VelocityX/Y/Z`、`AngularY`、
      `Upright`、`Grounded`），`config.py` 默认值与 `plugin.toml` 同步
- [x] `VrchatOscBridge.motion_feedback()` → `body_awareness.vrchat_osc.motion` 与
      `GET /snapshot` 的 `vrchat_osc.motion`。收不到时 `available=false` 并给出
      `reason`，**不退化成「速度为零」**
- [x] **新鲜度按链路年龄判，不按取值年龄判**。VRChat 参数是变化驱动的：站着不动时
      速度恒为 0，就不会再有新消息。若按取值年龄，站住几秒后「我没在动」反而变成
      「不可知」——正好把唯一能确认静止的时刻丢掉。取值年龄单列为 `value_age_ms`
- [x] `LocalNavigator` 卡墙判据：连续 `stall_ticks` 次发出前进指令但水平速度低于
      `stall_speed_mps` 时停车，`reason=movement_stalled`。速度用
      `hypot(VelocityX, VelocityZ)` 而非三维速度，否则「贴着墙往下滑」会被读成前进
- [x] 判定闩锁，只由换目标/重提目标解除。停下之后速度当然还是 0，靠速度自己永远
      解不开；绕行还是放弃交给 LLM，导航器不自作主张侧移
- [x] 收不到内置参数时判据整体失效并放行。把「读不到」当成「卡住了」，等于在没配
      这些参数的 avatar 上直接废掉导航——那是反方向的伪造
- [x] `instructions.py` 第 25、26 条；调试面板显示实测速度与卡住判定
- [ ] **用真机会话验证内置参数名**——`VelocityX/Y/Z` 等取自 VRChat 官方文档，但本仓库
      无法验证实机回传；内置参数只有在 avatar 的参数列表里存在时才会被驱动。名字对
      不上时表现为 `motion.available=false`，与「avatar 没配」不可区分
- [ ] **校准 `stall_speed_mps = 0.15` 与 `stall_ticks = 8`**——两个都是估算：Velocity
      的单位未经验证，`max_forward_axis = 0.28` 对应的实际速度也没实测过
- [ ] 障碍感知本身仍然没有。当前只能**撞上之后**察觉，不能提前绕开

### P3 — 补社交反应（第 7 行）

已完成的部分（`world_salience.py`）：

- [x] **唤醒通路本身**。此前主动世界推送一律用 `ai_behavior="read"`，宿主把它映射成
      `passive`——只装饰下一次由用户触发的回合，也就是说没有任何世界变化能让 agent
      开口。社交相关的 delta 现在走 `respond` + `priority=0` + `coalesce_key`，并受
      12 s 最小间隔约束；压下来的仍以 `read` 进上下文，不丢
- [x] **去重签名**。旧签名是 `(id, state, label)`，而 `state` 恒为 `"visible"`、检测器
      不发事件——「有人径直走过来」全程签名不变，产生零次推送。现在签名里加入量化后的
      距离档位与方位扇区。量化是刻意的：用原始浮点等于每帧都唯一，去重直接失效
- [x] 能察觉「有人靠近」「近处的人离开」

仍缺：

- [ ] **「有人挥手」实际上还不通**。`classify` 认这类事件，但本地检测器只输出检测框，
      不产生 `wave` / `point` 之类的事件，所以这条分支目前没有数据源
- [ ] 「有人在对我说话」——和「无音频通路」强相关，需要先决定要不要引入音频
- [ ] 唤醒判据的阈值（`_PROXIMITY_BANDS` 的档位、12 s 间隔）都是估计值，需要真机校准

### P4 — 接 LLM 到 cognition

- [ ] `cognition.py:599` 的确定性基线规划器已经定好 schema，LLM 输出对齐即可
- [ ] 前置条件：P1 完成，世界模型可信

## 明确不做

写在这里是为了防止「顺手也做了」。要动其中任何一项，先明确说明为什么现在值得。

1. **镜面倒影检测、avatar 与场景物件合并框修复。** 私人房 2~3 人的场景下这两个误差
   可以容忍；修它们要引入 OSC 位置交叉验证或掩码配置，复杂度不划算。
   ⚠️ 若将来扩到公共房，这两项会一起变成阻塞项——它们和已修的小框假阳性是**同一个
   失效模式**：污染 `apparent_height`，而导航距离闭环整个挂在这一个标量上。
1b. **二维人物立绘 / 海报被检成 person。** 这是一个**独立于**上一条的失效模式，不要
   归进「镜面倒影」一起搁置。真机实测（2026-08-21，帧见 `tmp/probe_frame.jpg`，
   叠框见 `tmp/overlay_check.png`）：墙上一张立绘海报被检出 `person`，
   **confidence 0.8168 是该帧最高分**，高于画面里任何真实 avatar。
   由此得到两个结论：
   - **抬 `confidence_threshold` 无法分离它**——它比真目标分还高，抬阈值先杀真人。
   - 时序一致性过滤（静止 N 秒降权）**也不可用**：会误伤站着不动聊天的人，而私人房
     里这恰好是最常见的状态。
   真正需要的是深度或多视角一致性，两者当前都没有（检测器无深度头）。
   现状是导航器可能把海报当人走过去。**只记录，不修。**
2. **实时反射类玩法**（节奏光剑等）。agent 决策是秒级节拍，这类玩法要求把策略下沉进
   navigator 那样的本地循环，不是把 agent 塞进去。不在本路线图范围内。
3. **语音输入 / 输出。** 目前全仓库没有音频通路。P3 可能会重新提出，但不要顺手引入。
4. **自动好友、邀请、社交图谱、世界切换。** `autonomy` 明确禁止，不是待办项。
5. **公共房 / 高人数密度优化。** 部署目标是私人房 2~3 人。
6. **为某个特定模型做特化。** 见北极星最后一句。

## 不可违反的约束

这套架构是围绕「不知道就说不知道」建起来的，以下是它的具体形态：

1. **不伪造。** 缺模型 → `available=false` 而不是猜实体；没深度头 → 距离 `unknown`
   而不是估一个；标签文件认不出来 → 抛错而不是静默回落 COCO。
2. **帧只喂给 agent 理解，不写进 `world_state`。** 给 LLM 塞原始像素会把编造风险请
   回来（盯着 512px 模糊截图是会脑补的）。从画面得出的结论一律是低置信视觉猜测，
   不能冒充观测，**尤其不能拿去满足 `body_reach_and_grab` 的 `preconditions`**——那条
   路必须继续走 entity_id + confidence + max_age 的结构化通道。
   这样 `instructions.py` 第 15、22 条不用改，依然成立。
3. **感知不进 120 Hz 调度线程。** worker 用有界 latest-frame 队列，掉帧优先于堆积。
4. **手动 arm 门禁不绕过。** 自主动作需要用户显式授权当前会话 + 会话 TTL。
5. **`accepted=true` 只代表已入队/已发送**，不代表已到达、已移动、已拿到。
   唯一的例外通路是 `body_awareness.vrchat_osc.motion`——它是 VRChat 回传的实测速度，
   但它自己也遵守第 1 条：收不到就是 `available=false`，不是「速度为零」。
