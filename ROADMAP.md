# 路线图

本文件的作用是**防止方向漂移**。改动方向前先读「明确不做」和「不可违反的约束」
两节；要往里加东西，先确认它服务于北极星，而不是只是「看起来该做」。

## 北极星

**让一个通用 agent 能流畅地玩 VRChat。**

验收标准就是字面意思：把任意LLM agent接上这套身体，它能在私人房里
站着聊天、跟着人走、注意到有人挥手并回应、被搭话时转过去。做到这个程度，换别的
agent 也不会有结构性障碍——所以不为某一个模型做特化。

「流畅」的边界见「明确不做」第 2 条：这条路径覆盖的是**社交向**玩法，不是实时反射
类玩法。

## 能力阶梯（截至 2026-08-26）

| # | 能力 | 状态 | 说明 |
|---|------|------|------|
| 1 | 在场 / idle 自然 | ✅ | VMC 待机中转 + VMD 语义片段 + `body_express` |
| 2 | 文字交流 | ✅ | `body_chatbox`。**无语音**——全仓库没有音频通路 |
| 3 | 姿态 / 手势 | ✅ | 调度器 120 Hz，安全子集比驱动本身更严 |
| 4 | 走向可见目标 | ⚠️ | 10 Hz 闭环 + 内置 Velocity 卡墙判据（参数名与阈值已真机验证：空地 4.0 m/s vs 顶墙 0.08 m/s）；撞过的方向短期不再重选（`direction_memory`，阈值未校准）；**仍无障碍感知**，只能撞上之后察觉 |
| 5 | 手部交互 | ⚠️ | 能发握持输入，`object_held` 永远 unknown，不能确认拿到 |
| 6 | 看懂场面 | ⚠️ | 已能看到像素并叠检测框（`vrc_vision_frame --overlay` / `debug_cli vision-frame`）；检测器本身仍无深度/OCR；**假阳性占多数**——实测一帧 7 个实体里 5 个确定是 avatar 菜单缩略图，另一帧 3 个实体全是植被/招牌/镜面倒影（见「明确不做」第 1、1b 条） |
| 7 | 社交反应 | ⚠️ | 能被「有人靠近 / 有人挥手 / 近处的人离开」叫醒；挥手仍依赖检测器发事件，本地检测器目前不发 |

### 第 6 行原本为什么是缺口

`tool_defs.py` 里 30+ 个工具，曾经没有一个返回像素。`world_observe` 给 agent 的是压平后
的结构化快照（label / bbox / confidence / bearing_deg / apparent_height），连 VLM 的理解
也在后端就被压成 entity/event 才送出去。

这对 10 Hz 反射式导航循环是**正确设计**（它要的就是有界标量），但对一个要「玩」的
agent 等于蒙眼递坐标表：知道「-12° 有个 person，画面高度占比 0.4」，不知道对方在挥手、
在指什么、自己是否卡在墙角、房间在办活动还是闲聊。

P0 补上的是这条并行通道——`vrc_vision_frame` 与唤醒配图让 agent 看得到像素，而结构化
快照原样保留给导航器。两者**不合流**：像素只喂理解，`world_state` 仍然只由检测器写入。

## 下一步（有序）

### P0 — 让 agent 能看见

- [x] `vrc_vision_frame` 工具：`VisionRuntime` 单槽帧缓存（`process_frame` 里、
      `_release_frame` 之前编码）→ `GET /vision/frame` → `RemoteVision.frame` → 工具。
      图不能走工具返回值（`Ok` 只是 JSON，模型看不到里面的 base64），所以用
      `push_message` 的 image part 让宿主注入当前回合
- [x] 唤醒推送带图：被判定为社交相关的 delta 走 `respond` 时附一张 ≤2 s 的画面；
      `read` 推送不配图——宿主对 read 的图是立刻注入的，配上 2 Hz 节奏就是刷屏
- [x] 限流：`frame_max_per_minute`（默认 10，滑动窗口，见 `frame_budget.py`）。
      没用令牌桶——桶允许攒额度，安静十分钟后能一口气连拉十张，恰好是最该拦的突发
- [x] `instructions.py` 增加对应约束条款（第 23、24 条）
- [x] 可选叠加检测框，用于对照「检测器看到的」与「画面里实际有的」。
      `latest_frame(overlay=True)` → `?overlay=1` → `RemoteVision.frame(overlay=True)` →
      `vrc_vision_frame` 的 `overlay` 参数。框画在**解码后的副本**上，绝不写回帧缓存：
      唤醒推送用的是同一份像素，烧了框就再也还原不回来。
      归一化→像素的换算拆成纯函数 `overlay_boxes_geometry`，好在没有 Pillow 的机器上测
- [x] **叠框必须报出帧与框的时间差。** 帧缓存按 `frame_cache_interval_s` 限流，世界快照
      却是最新的——把一秒前的像素配上现在的框，等于声称「这个人现在在这里」。
      返回值带 `overlay.skew_ms`，超过半个采集间隔时在图顶烧一条红色警告条，
      而不是只放进 JSON：JSON 字段会被忽略，画在图上的不会
- [x] `debug_cli.py vision-frame [--out] [--overlay] [--max-age-ms] [--quiet]`。
      **这一条才是「能看见」真正落地的地方**：在此之前唯一的看图路径是插件里的
      `push_message` image part，它需要 N.E.K.O 宿主在跑，于是在 debug_cli 工作流下
      P0 已完成项对 agent 完全不可用——只能读到检测器压平后的标量。附带补上
      `vision-start` / `vision-stop`（`perception` 早已存在，不需要 `vision-status`）
- [ ] 取到的帧顺带落盘，自然积累回归语料（当前只在显式 `--out` 时落盘，是人工 dump，
      `raw_frames_persisted` 仍为 `false`）
- [ ] **用真机会话验证这条路真的通**——单元测试只覆盖到 `push_message` 之前；
      宿主 `stream_image` 那一段（会话存在与否、图过大时的重压/丢弃）没有被测到，
      `frame_submitted=true` 只代表已提交给宿主

排在 P4 前面的理由：cognition 那个规划器接缝假设下面已经有可信的世界模型，而现在
没有。而 agent 一旦能看图，它自己就变成了排查其余各项的仪器。

### P1 — 用真机会话验证感知

- [x] **确认 27×27 px 假阳性是在哪个坐标系量的**——问题不成立：预处理是无 letterbox
      的直接拉伸（`local_perception.py` 的 `cv2.resize`），归一化按轴各自进行，所以
      模型空间和采集区域的**归一化每轴比例是同一个数**。但由此得到的推论要记牢：
      标定必须分别记录宽比和高比，一个数不够
- [x] 据此拆分 `min_box_ratio`：改为按轴设阈值。共用 2% 时总是宽度先卡（1920 下最小
      宽 38 px），按人体长宽比反推，能进入世界的最小 `apparent_height` 有 7%~11%，
      而导航器目标是 0.55——房间对面的人被静默丢掉。现默认宽 0.8% / 高 2%
- [ ] **用真机会话校准这两个默认值**——它们仍是按几何反推的估计，不是实测；单元测试
      全绿不能说明问题
- [ ] 攒真实帧回归集：正常 2~3 人 / 带镜子 / avatar 与场景物件重叠 / 空房
- [x] 量化镜面倒影与合并框的发生率（**只测不修**，理由见「明确不做」第 1 条）。
      2026-08-21 首次用叠框图实测，两帧结论见「明确不做」第 1 与 1b 条。要点：
      **假阳性占多数，且不是「偶发误检」而是三类固定来源**——UI 缩略图、场景植被/招牌、
      镜面倒影。「合并框」这一项反而**没有观测到**：此前怀疑被拆成两个实体的
      `track:97`/`track:98`（−28.8° / −29.0°，`apparent_height` 0.093 / 0.094）
      叠框后看清是**同一列里上下相邻的两张 avatar 选择器缩略图**，既不是一个人被拆成
      两框，也不是两个人

### P2 — 让身体知道自己有没有动（第 4 行）

在此之前，全仓库没有任何一条通路能回答「我真的移动了吗」：AnyaDance 与 OSC 的
`accepted=true` 都只代表**本机 UDP 发送成功**，UDP 也不会回执。于是导航器分不清
「正在前进」和「顶着墙推摇杆」，一头撞进墙里可以永远推下去。

数据其实一直在：`_handle_message` 缓存了收到的每一个 `/avatar/parameters/*`，只是
被 `awareness_parameters` 白名单挡在 awareness 之外。所以这不是新链路，是打开一扇
已经存在的门，再从中导出一个信号。

- [x] 白名单加入六个 VRChat 内置 Avatar 参数（`VelocityX/Y/Z`、`AngularY`、
      `Upright`、`Grounded`），`config.py` 默认值与 `plugin.toml` 同步
- [x] `VrchatOscBridge.motion_feedback()` → `body_awareness.vrchat_osc.motion` 与
      `GET /snapshot` 的 `vrchat_osc.motion`。收不到时 `available=false` 并给出
      `reason`，**不退化成「速度为零」**
- [x] **新鲜度按链路年龄判，不按取值年龄判**。VRChat 参数是变化驱动的：站着不动时
      速度恒为 0，就不会再有新消息。若按取值年龄，站住几秒后「我没在动」反而变成
      「不可知」——正好把唯一能确认静止的时刻丢掉。取值年龄单列为 `value_age_ms`
- [x] `LocalNavigator` 卡墙判据：连续 `stall_ticks` 次发出前进指令但水平速度低于
      `stall_speed_mps` 时停车，`reason=movement_stalled`。速度用
      `hypot(VelocityX, VelocityZ)` 而非三维速度，否则「贴着墙往下滑」会被读成前进
- [x] 判定闩锁，只由换目标/重提目标解除。停下之后速度当然还是 0，靠速度自己永远
      解不开；绕行还是放弃交给 LLM，导航器不自作主张侧移
- [x] 收不到内置参数时判据整体失效并放行。把「读不到」当成「卡住了」，等于在没配
      这些参数的 avatar 上直接废掉导航——那是反方向的伪造
- [x] `instructions.py` 第 25、26 条；调试面板显示实测速度与卡住判定
- [x] **用真机会话验证内置参数名**（2026-08-21）。实机回传的是 `VelocityX`、
      `VelocityY`、`VelocityZ`、`VelocityMagnitude`、`Grounded` 共 5 个；
      **`Upright` 与 `AngularY` 这个 avatar 不驱动**。名字本身全部正确。
      → **`AngularY` 这半句已被 2026-08-26 实测推翻**，见下面的角速度校准条目：
      它确实会回传，只是**仅在转向进行中**。2026-08-21 那次没转身，所以看到的是
      「不驱动」——同一个「站着不动就不发包」的坑，换了个参数重演一次。
      `Upright` 仍未观测到。
      两个必须记住的坑：
      - `motion.available` **不**依赖 `Upright`/`AngularY`——只要有任一速度轴且链路
        新鲜就为 `true`。此前把 `available=false` 读成「缺这两个参数所以判据废了」是
        误判：真实原因是**站着不动时 VRChat 不再发包**，于是 `reason=feedback_stale`。
        一动起来立刻 `available=true`（实测 `speed_mps` 2.95→4.0、`grounded=true`）
      - VRChat 只在 avatar 加载/切换时做一次参数全量下发。后端重启后若 avatar 没变，
        `received_packets` 会一直是 0、`avatar_id=null`——这是**正常**的，不是链路故障
- [x] **校准 `stall_speed_mps = 0.15` 与 `stall_ticks = 8`**（2026-08-21，实测）。
      `VelocityMagnitude` 的单位就是 m/s，量级验证如下：
      - 空地全摇杆前进（`vertical=1.0`）：**4.0 m/s**（跑速）
      - 顶着镜子推摇杆：**0.08 m/s**（`vertical=1.0`）/ 0.054（0.7）/ 0.011（0.3）
      两者差 **50 倍**，`0.15` 正好落在中间且两侧余量都很大——阈值成立，不必改。
      顺带确认摇杆幅值是被尊重的（0.3/0.7/1.0 单调对应 0.011/0.054/0.080），
      所以「卡住」表现为**幅值正常但速度塌到近零**，不是指令没发出去
- [x] **撞过之后不再往同一处撞**（`backend/direction_memory.py`）。这是介于「撞上才
      察觉」和「提前绕开」之间的第三种状态，不要和下一条混为一谈：它消除的是实测到
      最痛的行为——反复把同一堵墙选成 wander 目标。不建坐标系、不做里程计，只在 8 个
      45° 扇区上记账，每条记录带短 TTL。几个刻意的选择：
      - **扇区锚在调度器虚拟 HMD 的 yaw 上**，不是「相对当前朝向」。混用是个真实
        bug：撞墙时记下 `+45°`，转身之后同一个 `+45°` 已经指向别处，拿旧记录去拒绝
        会把可走的方向封死。虚拟 HMD yaw 是全仓库唯一连续、单调、我们自己驱动的朝向
        量，但它只在一个世界里有意义——换世界经 `on_world_changed` 整体 `reset()`
      - **预测与实测分字段保存**，不合并成单值 `state`。实测态只有
        `unknown`/`verified_free`/`blocked`，没有 `predicted_free`；两者冲突经
        `prediction_disagreements` 交回主 LLM，这正是最有价值的那条信息
      - `verified_free` 要求**门控积分出真实位移**，不是「收到过速度样本」。顶着墙时
        VRChat 照样回传速度包且值可能就是 0.0，而 0.0 是个有限数——按样本存在性判定
        会把撞墙记成走通，恰好和这套记忆想解决的问题相反（约束第 1 条的具体形态）
      - **刻意没有 `best_direction()`。** 一旦有它，导航器就又变回替 LLM 选路的那层。
        拒绝只对 `confident_block`（同一扇区撞过两次）生效，且只回理由、不替 LLM 换
        方向；闸门收在 `autonomy_wander_step` 消费路线任务**之前**，并同时守住
        `autonomy_goal(kind="wander")` 以防绕过工具层。approach/follow 不受影响——
        那两者的方向由目标决定
      - 对外只多一个 `constraints.direction_scores`（`instructions.py` 第 30 条），
        措辞明确写了它不是通行概率
- [x] **校准角速度门控的两个常量**（2026-08-26，实测）。`/osc/turn horizontal=0.6`
      叠加前进、10Hz 采样：转向中 `AngularY` 为 **-544 ~ -816**，停转后是**精确的
      0.0**，中间没有过渡值。三个结论：
      - 单位不明且量级远超任何角度/弧度解读，所以**继续只用它判「在不在转」**，
        不参与角度积分——原设计成立，不改
      - `_ANGULAR_RESTING` 从 `0.05` 改成 **`1.0`**。真实间隔是 0 与 544，不是
        「噪声底 vs 小信号」；取 1.0 是让它落在空档中央，而不是贴着噪声下沿
      - `_ANGULAR_FRESH_MS` 从 `250` 放宽到 **`400`**。这道门实测是**冗余的**：
        yaw 停止变化的同一拍 VRChat 就主动发 `AngularY=0.0`，不留非零尾包
        （停转后 `angular_age_ms` 从 78ms 单调爬到 1718ms，而 `|0.0|` 早已低于
        `_ANGULAR_RESTING`，速度项自己就把 `fresh_angular` 判成 False）。保留它
        只为防「非零包仍在缓存」这一**未观测到**的情形；既然不承担实际判定，就
        不该在采样被视觉拖慢时误杀新鲜样本
      两个单元测试的 `angular_speed` 夹具原本写的是 `0.8`（照旧阈值编的），已同步
      改到实测量级 `-544.0`
- [x] **校准 `_MIN_PROGRESS_M`**（2026-08-26，实机验证）。
      下界：空地直行段 `gated_progress_m=2.79`，`recorded_state=verified_free`，
      0.37 m 门槛被正确穿过；巡航速度 1.5556 m/s，1.1111 m/s 是 vertical=0.35 时的值。
      上界：实机测试发现 `blocked` 的判定路径并**不经过距离门槛**——`stall_detected`
      和 `wall_slide_detected` 独立判定受阻，距离门槛 `_MIN_PROGRESS_M` 只影响
      `cleared`（即走通的正向判定）。顶着墙推时 recovery 之后的绕行位移混入
      `gated_progress_m`（实测 4.25~5.94 m），这些距离描述的是绕行后的自由空间，
      不是「撞墙段本身积了多少」。结论：0.37 m 的上界没有实际意义——真实撞墙由
      `stall`/`wall_slide` 判定，距离门槛只防「起步抖动被记成走通」，而 0.37 m
      远高于起步抖动的量级（实测贴墙滑行 3 拍只积约 0.21 m）。**此项视为已足够，
      不需要继续校准。**
- [ ] **校准 45 s / 5 s / 撞 2 次三个阈值**——按巡航速度与 wander 段长几何反推的
      估计值，没有经过真机验证；单元测试全绿不能说明问题。
      同 P1 的 `min_box_ratio` 默认值与 P3 的唤醒阈值，属于同一类待校准项。
      注意上面那条只校准了**角速度门控与距离门槛**——门控决定「哪些拍算进里程」，
      距离门槛决定「积出来的里程算不算走通」，这三个决定「多久忘记」与「撞几次确信」
- [ ] 障碍感知本身仍然没有。当前只能**撞上之后**察觉，不能提前绕开——上一条记的是
      「撞过哪儿」，不是「前面有什么」
- [ ] **`stall.detectable` 的语义与注释不符，需要修。** `navigator.py:384` 的注释写着
      `detectable=false` 表示「收不到 VRChat 内置 Velocity 参数」，但实际它等于
      `bool(self._last_motion and ...available)`，而 `_last_motion` 只在
      `decision.state == "advance"` 时才由 `_sample_motion()` 填。于是 autonomy 未
      arm 时它恒为 `false`——**含义是「导航器从没采样过」，不是「参数收不到」**。
      2026-08-21 实测正是这个组合：`motion.available=true`（速度 4.0 m/s 在手）而
      `stall.detectable=false`。这个歧义会让人（包括 agent 自己）以为卡墙检测能力
      不存在，而它其实是好的，只是没跑

### P3 — 补社交反应（第 7 行）

已完成的部分（`world_salience.py`）：

- [x] **唤醒通路本身**。此前主动世界推送一律用 `ai_behavior="read"`，宿主把它映射成
      `passive`——只装饰下一次由用户触发的回合，也就是说没有任何世界变化能让 agent
      开口。社交相关的 delta 现在走 `respond` + `priority=0` + `coalesce_key`，并受
      12 s 最小间隔约束；压下来的仍以 `read` 进上下文，不丢
- [x] **去重签名**。旧签名是 `(id, state, label)`，而 `state` 恒为 `"visible"`、检测器
      不发事件——「有人径直走过来」全程签名不变，产生零次推送。现在签名里加入量化后的
      距离档位与方位扇区。量化是刻意的：用原始浮点等于每帧都唯一，去重直接失效
- [x] 能察觉「有人靠近」「近处的人离开」

仍缺：

- [ ] **「有人挥手」实际上还不通**。`classify` 认这类事件，但本地检测器只输出检测框，
      不产生 `wave` / `point` 之类的事件，所以这条分支目前没有数据源
- [ ] 「有人在对我说话」——和「无音频通路」强相关，需要先决定要不要引入音频
- [ ] 唤醒判据的阈值（`_PROXIMITY_BANDS` 的档位、12 s 间隔）都是估计值，需要真机校准

### P4 — 接 LLM 到 cognition

- [ ] `cognition.py:599` 的确定性基线规划器已经定好 schema，LLM 输出对齐即可
- [ ] 前置条件：P1 完成，世界模型可信

## 明确不做

写在这里是为了防止「顺手也做了」。要动其中任何一项，先明确说明为什么现在值得。

1. **镜面倒影检测、avatar 与场景物件合并框修复。** 私人房 2~3 人的场景下这两个误差
   可以容忍；修它们要引入 OSC 位置交叉验证或掩码配置，复杂度不划算。
   ⚠️ 若将来扩到公共房，这两项会一起变成阻塞项——它们和已修的小框假阳性是**同一个
   失效模式**：污染 `apparent_height`，而导航距离闭环整个挂在这一个标量上。
   **2026-08-21 实测补充**（帧见 `tmp/back_1.jpg`）：镜面倒影确实会被检成 `person`
   （conf 0.55，框正压在世界里那面写着「Avatar Mirror」的镜子上）。同一帧还有两个
   **场景物件**假阳性：棕榈植被（conf 0.64）与展板招牌（conf 0.58）。该帧共 3 个实体，
   **真人 0 个**。「合并框」在两次实测里都没有出现，见 P1 对应条目。
1b. **二维人物立绘 / 海报被检成 person。** 这是一个**独立于**上一条的失效模式，不要
   归进「镜面倒影」一起搁置。真机实测（2026-08-21，帧见 `tmp/probe_frame.jpg`，
   叠框见 `tmp/overlay_check.png`）：墙上一张立绘海报被检出 `person`，
   **confidence 0.8168 是该帧最高分**，高于画面里任何真实 avatar。
   由此得到两个结论：
   - **抬 `confidence_threshold` 无法分离它**——它比真目标分还高，抬阈值先杀真人。
   - 时序一致性过滤（静止 N 秒降权）**也不可用**：会误伤站着不动聊天的人，而私人房
     里这恰好是最常见的状态。
   真正需要的是深度或多视角一致性，两者当前都没有（检测器无深度头）。
   现状是导航器可能把海报当人走过去。**只记录，不修。**

   **2026-08-21 二次实测把这一条的规模改写了**（叠框见 `tmp/f_overlay.jpg`）：
   最大的假阳性来源不是墙上的海报，而是**游戏内的 avatar 选择器菜单**——菜单打开时
   左侧是一整片 avatar 头像缩略图网格，每张有人形的缩略图都被检成一个 `person`
   实体。该帧 7 个实体里 **5 个确定是 UI 缩略图**（conf 0.46~0.68，方位全部挤在
   −22°~−29°）。剩下 2 个（conf 0.80 / 0.83）是场景里的 3D avatar，但**无法确定是
   真人还是镜面倒影**：其中 +18° 那个是半透明的，而随后走过去发现那个方向正对着一面
   镜子（见第 1 条）。也就是说这一帧里能确认的真人数量是 **0~2，取不到确定值**。
   这比「一张海报」严重得多，也带来一个新结论：
   - **假阳性会成簇出现在同一个方位扇区**，因为缩略图在网格里同列。导航器看到的是
     「那个方向有五个人」，而那里是一块 UI 面板。P3 的方位扇区去重签名会把这一簇
     压成一次唤醒，但方位本身仍然是错的。
   - 与海报不同，这一类**有廉价的判据**：菜单是自己打开的，UI 开合状态本机可知。
     真要处理，成本最低的做法是「菜单打开时不信任视觉」，而不是改检测器。
     **仍然只记录，不修**——但记下这条判据存在，将来需要时不必重新发现。
2. **实时反射类玩法**（节奏光剑等）。agent 决策是秒级节拍，这类玩法要求把策略下沉进
   navigator 那样的本地循环，不是把 agent 塞进去。不在本路线图范围内。
3. **语音输入 / 输出。** 目前全仓库没有音频通路。P3 可能会重新提出，但不要顺手引入。
4. **自动好友、邀请、社交图谱、世界切换。** `autonomy` 明确禁止，不是待办项。
5. **公共房 / 高人数密度优化。** 部署目标是私人房 2~3 人。
6. **为某个特定模型做特化。** 见北极星最后一句。

## 不可违反的约束

这套架构是围绕「不知道就说不知道」建起来的，以下是它的具体形态：

1. **不伪造。** 缺模型 → `available=false` 而不是猜实体；没深度头 → 距离 `unknown`
   而不是估一个；标签文件认不出来 → 抛错而不是静默回落 COCO。
2. **帧只喂给 agent 理解，不写进 `world_state`。** 给 LLM 塞原始像素会把编造风险请
   回来（盯着 512px 模糊截图是会脑补的）。从画面得出的结论一律是低置信视觉猜测，
   不能冒充观测，**尤其不能拿去满足 `body_reach_and_grab` 的 `preconditions`**——那条
   路必须继续走 entity_id + confidence + max_age 的结构化通道。
   这样 `instructions.py` 第 15、22 条不用改，依然成立。

   **2026-08-26 补充边界。** 这条此前只点名了 `world_state` 一个去处，而
   `direction_memory` 的 `constraints.direction_scores` 让后端出现了第二个能容纳视觉
   猜测的容器（主 LLM 看图给出的方向偏好）。它被判定为合规，靠的是隔离而不是位置，
   所以隔离的形态必须写下来：**预测可以存，但必须与实测分字段保存、有独立且更短的
   TTL、且不得单独构成执行依据。** 具体到当前实现——`should_refuse()` 只看实测的
   `confident_block`，从不读预测；实测态里没有 `predicted_free`。
   把预测合并进单个 `state` 字段是这条边界的**反面**，不管它落在哪个容器里：那会抹掉
   「你说能走、实际撞了」这个分歧，而分歧本身才是要交还给主 LLM 的东西。
3. **感知不进 120 Hz 调度线程。** worker 用有界 latest-frame 队列，掉帧优先于堆积。
4. **手动 arm 门禁不绕过。** 自主动作需要用户显式授权当前会话 + 会话 TTL。
5. **`accepted=true` 只代表已入队/已发送**，不代表已到达、已移动、已拿到。
   唯一的例外通路是 `body_awareness.vrchat_osc.motion`——它是 VRChat 回传的实测速度，
   但它自己也遵守第 1 条：收不到就是 `available=false`，不是「速度为零」。
