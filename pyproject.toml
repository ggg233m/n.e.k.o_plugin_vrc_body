[project]
name = "neko-yui-npc-controller"
version = "0.2.0"
description = "N.E.K.O plugin for the YUI world-native NPC protocol"
requires-python = "==3.11.*"
dependencies = [
  "mido>=1.3,<2",
  "python-rtmidi>=1.5,<2",
]
