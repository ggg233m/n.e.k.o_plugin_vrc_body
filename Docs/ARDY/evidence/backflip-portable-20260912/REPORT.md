# 后空翻复测与跨机交付

2026-09-12，当前Unity主场景ClientSim、真实ARDY Core40、Intel Arc A770 XPU与NEKO_MIDI。未启动VRChat。沿用NavMesh区域/软接触策略，不通过手工停用注视或Animator绕过控制权仲裁。

## 后空翻：233216

先完成到大厅的真实路径，然后3个种子分别新建运动会话并尝试10秒后空翻。每次从当前实际位置/姿态继续，不是严格相同初始状态的A/B实验。

| 尝试 | 结果 | 实际倒立采样 | 倒立区间 | 结束骨盆向上分量 |
|---|---|---|---|---|
| 20260912 | stream_underrun，未完成 | 无完整执行区间 | 不判定 | 不判定 |
| 20260913 | 世界完成，9.947秒 | 21 | 2 | 0.9997432 |
| 20260914 | 世界完成，9.990秒 | 9 | 1 | 0.998224735 |

两次成功尝试水平位移约3.142m和2.243m，均实际倒立并在结束恢复直立。图像127/147显示第二次尝试存在两段翻转，不符合“严格一次”；153显示其结束站姿。192/198显示第三次尝试的一段倒立翻转及结束站姿。头发衣物和身体方向可以从图像复核，不能只数输入模型帧。

模型生成与世界可视播放可行，但稳定单次后空翻仍未通过。当前根仍按地面支撑执行，空中根碰撞/腾空落地物理没有因此完成；本轮也没有鞋底接触测量，恢复直立不等于可靠双脚落地。失败候选完整保留，整轮partial。

原始事件、模型块、渲染采样和图像分别见同目录四个运行文件夹。`backflip-retest-picks.json`记录观察图像索引。测试后退出Play并关闭本轮自有服务，场景持续开关仍默认关闭。

## 跨机实现

项目新增相对路径启动入口、CUDA/XPU隔离配置、依赖安装器、派生LoRA路径配置、离线模型加载、完整性检查及迁移导出。默认图重放组件已从F盘实验目录收进项目；XPU编译器通过vswhere发现，CUDA不加载XPU图重放代码。缓存和新机令牌不从旧电脑复制。

本机入口check已经通过；原有环境pip check无损坏依赖，4项目录/设备/缺失分片测试通过。后续换目录、干净环境与模型生成的实测记录追加于下方。只有本机XPU可实际运行，CUDA没有第二台显卡实测，不把接口支持等同硬件性能通过。

## 2026-09-13 交付范围调整与源码安装

用户明确要求ARDY由使用者自行下载，本次最终交付改为轻量接入包，不交付完整ARDY源码、ARDY wheel或模型。此前18GB导出目录仅保留为本机测试材料，不作为交付。

轻量方案包含49个相对官方提交693f74d13b3d04a0a22ce127ee79c929dd89756b的补丁文件、独立后台、启动安装工具、CUDA/XPU配置、viser依赖和NEKO/Unity集成。使用者下载对应官方源码及权重，可直接配置已有资源目录。补丁全部预检后才备份写入；版本冲突保持原文件，CRLF兼容与重复应用验证通过。

从官方Git对象导出干净基线后，49/49补丁成功应用；在新Python环境使用MSVC/Ninja从该源码成功构建ARDY wheel（仅用于本机安装验证，不分发）。66个Python文件与源码逐字一致，新编译的motion_correction原生扩展导入通过。相关原始日志：portable-source-build.log、portable-source-build-check.json。路径/设备/补丁测试共5项通过，PowerShell脚本语法检查通过。

独立环境安装发现并修复了旧ARDY wheel落后于当前量化代码的问题；最终方案改用下载源码应用补丁再构建，消除了该旧wheel来源。Windows PowerShell编码与中文路径、编译器发现、系统盘临时目录和首次启动等待分别修正。启动等待仅用于模型初始化，不改变推理请求3秒时限或世界控制权约束。新机冷初始化上限默认3600秒，加载期间ready=false。

CUDA安装和标准推理分支保留，当前没有NVIDIA硬件验证；XPU图重放不用于CUDA。没有启动VRChat、没有重载活动NEKO宿主、没有提交或推送仓库。

迁移验证另外发现自检脚本在最终自由动作调用中漏传origin_xz。完整模型与图/历史预热已通过，但该次最终检查以KeyError失败，原始记录保存为portable-relocated-smoke-cold-incomplete.log。现已补齐局部原点并复跑，不把失败运行标成smoke通过。冷运行仅量化/加载约1152秒、普通/路径窗口首次编译约735秒，此外还有连续历史首次编译；其中存在同时进行导出校验的磁盘竞争，不作纯性能基准。

## 实际XPU生成复跑通过

portable-relocated-smoke.json：Core40图重放，40帧、20Hz，输出形状[1,40,330]，全部有限数值。缓存后的模型加载与完整预热200.9766秒，该次预热后生成0.38756秒。这是同一台A770上换目录、新Python环境且最初无模型缓存的验证，不能称为第二台电脑或CUDA硬件验证，也不是P95统计。

模型预热与实际输出通过后，另外使用轻量包入口、外部源码/模型路径与新环境验证独立后台。该入口检查使用Core40Eager以隔离子进程/路径问题，与以上Core40图重放生成分别记录；最终health结果见portable-light-service-health.json。

## 最终交付

轻量包：ARDY-NEKO-integration-20260913（约34MB）。ARDY源码和模型由使用者自行下载，包中只保留补丁及接入内容。后台入口Core40Eager实测约72.526秒达到health.ready=true、error=null，未绑定世界故execution_ready=false。测试服务正常关闭，2346端口已释放。

主项目PORTABLE.md与计划已同步；本轮没有修改Unity场景及身体执行器，也没有新增主模型工具或运行时复杂逐帧校验。详细本机验证摘要随轻量包portable/VALIDATION.md交付。

最终轻量压缩包：H:/AI/NVidia/deliverables/ARDY-NEKO-integration-20260913.zip，30,992,616字节。目录共329个清单文件、34,283,628字节，全部SHA256校验通过，ZIP CRC检查通过。ZIP SHA256：c968bb632be179b5321bcf636f575f9661fee1d457f1b2e273c8eade7a205bf0。确认无完整ARDY工程、ARDY wheel、模型权重、个人环境、GPU缓存或凭据。
