# ARDY 持续接管实施与验收

以用户2026-09-12批准的持续接管、多模式方案为准。旧PLAN中的阶段记录保留作历史证据，不能视为本轮验收结果。

## 最新结果：卡顿恢复、人工急停反例与4.62米行走

2026-09-12，主场景ClientSim真实NEKO_MIDI，未启动VRChat。证据包 `evidence/recovery-complex-20260912/` 保留11次测试的原始结果、事件、渲染后采样及失败记录，不能只挑成功轮次代表稳定性。

- **通信恢复通过**：061403注入1201ms真实Unity主线程停顿，旧任务取消、不补播，重新握手创建新运动会话；恢复流程用时0.859秒（从开始恢复流程计，不是从画面恢复瞬间计）。随后4秒任务实播4059ms完成，正常关闭无急停。061128曾因收据异常路径误用人工停止失败，已改为故障停止并加回归测试。
- **人工急停反例通过**：062645同样注入1201ms停顿，故障后人工急停，观察6秒无自动新会话，世界急停和宿主阻止标志保持。未覆盖原生MIDI句柄损坏、真实VRChat恢复与完整断连矩阵。
- **行走完整完成一次**：063920路径约4.6米，从画廊出发到长凳前，实际根位移4.623米、路径累计4.788米，世界返回 `npc.operation_completed`，任务实播11618ms；随后待机3秒并正常退出。接管期间Animator启用采样为0。脚部相对根有运动，不能据此认定鞋底脚滑P95或物理推蹬通过。
- **行走修复与失败**：原中心射线在高低地面接缝读到低面，胶囊向下撞到仍承重的高面（WALLS.021，向上法线），误报obstacle。现保留射线检验实际坡度，新增同半径球扫确定支撑高度；避免球扫边缘圆弧法线误作陡坡。墙体胶囊碰撞和原高度/接触安全限值保留。中间测试仍记录一次stream_underrun、一次16.8cm骨盆纠正超限及单球扫版本的unsupported_ground。最终版本尚未连续成功10次。
- **后空翻未完成**：062056提示词已受理并进入动作，随后因自由动作超出起点周围1米被 `stream_path_constraint` 中止，图像未完成翻转。这不证明ARDY模型不支持后空翻；当前执行器将根高度约束在地面，骨盆偏移限±0.3米，缺少腾空、飞行碰撞与落地阶段，尚不能发布后空翻能力。061801在基础待机阶段已失败，不计作有效后空翻动作测试。

本轮Python回归405 passed、1 skipped、301 subtests passed（16.67秒），Udon编译无错误。正式场景持续能力保持默认关闭；活动宿主插件未重载发布。恢复不增加正常逐帧包或ACK；地面修正增加一次Unity本地球扫，无MIDI带宽增量。下一步优先解决步态接触与根轨迹一致性、重复行走稳定性，再实现三维腾空和落地执行链。

## 前一轮结果：发送空档修复后100次切换通过

2026-09-12 055106，主场景真实MIDI、Core40、固定种子20260912，原Life及自主系统启用。100个意图实际执行，99个被替换任务收到正确取消回执，无旧任务错误完成；最后4秒动作世界实播4004ms。之后持续待机30秒并正常关闭，无急停。完整首帧可见延迟P95为1.259秒、最大1.294秒；接管期间Animator开启采样为0，健康HTTP无错误。证据见`evidence/early-control-20260912/`。

修复包括：在旧帧接收完毕、候选尚未生成时提前发送原有新意图边界标记；同一版本只发一次，不新增续租往返。新意图不再重复做整块矩阵校验/编码，确认后仅重绑代次与CRC。三个Udon接收器的CRC由逐位循环改为固定16项查表，协议长度、校验强度、998事件预算、112未确认上限、200ms提交边界和500ms授权保持不变。

全量Python回归400 passed、1 skipped、301 subtests passed（15.45秒），Udon编译无错误。正式场景持续能力仍关闭；复杂动作、30分钟运行、严格30Hz骨骼角度与鞋底脚滑指标仍未通过。本节更新下方历史记录中“100次切换未通过”的状态，不把单次链路成功扩大为完整动作质量通过。

## 实施状态

- [x] 实施前备份现有主项目改动、Unity脚本与SAVE场景，生成清单并验证ZIP。
- [x] 原身体辅助写入者与ARDY统一控制权门控，进入/退出300ms混合候选实现。
- [x] 状态生成版本、普通状态队列合并、ACK重放和迟到快照防倒退候选；生命周期现已共享版本并保护操作终态。
- [x] Core40持久候选历史、20帧检查点、提示词缓存按内容更新、过期结果拒绝。
- [x] 200ms提交块及有限任务后预生成待机的服务端时间线；逻辑100次切换测试。
- [x] 持续HTTP接口、v2控制帧、Unity接收缓冲和应用进度回执候选实现。
- [x] 宿主双向能力门控、正常退出与急停区分、主场景默认关闭接线。
- [ ] 主场景持续待机、路径与自由动作真实验证；真实100次切换。
- [ ] 普通关闭/同后端实例重连自动握手；急停锁存、超时及脱机完整验收。
- [ ] 主模型npc.perform可选模式/结束条件和自主探索纯意图接入完整验收。
- [ ] 路径中换目标、速度/朝向、多条件组合及实际到达条件。
- [ ] 全身关键帧和末端约束统一坐标与可达性；模型侧候选接口已写，世界尚未验收。
- [ ] 场景标注座椅、抓握与释放，正常/失败各类场景。
- [ ] 根位移3D、台阶支撑序列、跳跃和攀爬的安全收尾。
- [ ] 鞋底参考点、脚趾滚动、摆动脚碰撞、动态平台接触锚点。
- [ ] 长时间时间戳、局部坐标窗口重定位及30分钟运行性能记录。
- [ ] 活动NEKO宿主部署和完整语义规划到世界完成链路验收。
- [ ] 双客户端和晚加入：按用户安排延后，当前不启动VRChat。

## 验收标准（未通过项不能发布capability）

主场景固定种子、初始姿态和观察视角；原注视与自主系统保持正常启用。待机头部30Hz相邻差<=5度，进入/退出非意图骨骼跳变<=10度；预热后首个可见执行延迟P95<=2秒。4秒动作实际播放误差<=10%。100次实际切换无重复骨骼写入、旧帧复活或错误终态。

30分钟连续运行无意外欠载，记录CPU/GPU/内存/帧时间/停止延迟。鞋底相对支撑面滑动P95<=1cm、最大<=2cm，不用踝部IK误差替代。座椅、抓握、台阶、跳跃、攀爬、动态平台正常各10次，目标失效、受阻、接触丢失、切换、断模型和重连均有失败用例。

离线启动仍工作；关闭ARDY恢复原系统；推理卡住、世界断开停止授权；急停后重连不能自动解锁。字幕不驱动身体动作，主工具仍仅npc.perform与npc.stop，环境去重直接注入。

## 当前证据边界

Core40真实候选生成已验证20帧历史不被新意图污染，新提示词生成约0.462秒；新增固定形状首次执行预热约198.62秒。此数据不是NPC首帧延迟或动作质量。Python回归与Udon编译通过也不替代主场景实际动作验收。

## 主场景实测发现（持续更新，保留失败）

1. 020608：实际执行8模型帧后stream_underrun，原因是每块等待应用日志才发下一块。已改为预收/锁定/实际应用三个水位。
2. 022407：握手后积压10条player.pose挤占遥测窗口。已按分页合并最新玩家姿态，持续期间普通遥测最多2条/窗口，20条总上限不变。
3. 023227：持续待机约4秒通过，任务切换前旧待机在序号44触发额外写死的3cm根范围。候选已按计划采用1m活动区域，碰撞和足部约束保留；开始补充明确世界失败释放与端口信用结清，避免失败再变成急停。

4. 023753：真实持续待机→有限任务→任务后待机→正常退出首次通过，世界任务elapsed_ms=3954，退出没有急停。后续渲染后观察仍发现头部33度、退出最大160度，因此链路成功不代表质量通过。
5. 025642：固定种子20260912，加入待机朝向约束、根朝向同步、全量退出目标缓存和过渡角速度限制后，真实链路再次成功，elapsed_ms=3949。渲染后采样进入/退出最大约8.002度，稳定待机头部最大0.529度；原Life启用，ARDY独占期间Animator开启采样数为0。采样间隔约38–43ms，不能宣称严格30Hz验收通过；启动后左手仍有20.77度变化待诊断。
6. 路径终态候选增加实际距离<=15cm与目标存活/位移检查，NavMesh路径高度由碰撞地面校验，正在实测。

上述均为Unity主场景真实MIDI。v2只在Play Mode临时启用，保存场景仍关闭；尚不发布完整持续多模式能力。

## 后续修复与阻断记录

- 030326 路径切换：新意图首帧在路径请求后约0.83秒到达，超过0.8秒准备窗口，世界报stream_underrun；没有错误完成回执。候选已将准备预算调整为1.2秒，200ms不可修改窗口不变，等待重测。
- 030943 握手失败：Unity的Telemetry与Locomotion已因Udon执行超过10秒被VM停用。发生于后台重新预热期间，尚未证明根因；未提高VM超时掩盖问题。重建Play Mode后再测，不计入动作测试结果。
- 同实例后端恢复：健康不可用→恢复边沿触发新会话握手，持续健康的故障不无限重试，急停仍禁止接管。健康边沿/急停回归通过，活动宿主实测待做。
- 本次全量Python回归：371 passed, 1 skipped, 301 subtests passed（15.65秒）。不替代真实执行和质量验收。

- 031217：路径收到实际完成回执（1320ms），随后待机因stream_underrun退出。不能算持续路径模式成功。
- 031707：引入112事件信用内的两帧流水后，路径执行到116模型帧触发pelvis_correction_limit。未放宽12cm安全上限。仍有接触锁定/重定向问题；新增故障帧回滚，防止失败的中间姿态成为退出起点。
- 流水边界与回归：374 passed, 1 skipped, 301 subtests passed（13.37秒）。缺ACK时112普通事件+2急停事件，错误身份不能释放信用。
- 跨运行时QPC出现偏移，本轮首帧延迟数值不采用；后续同时采集公共UTC基准并检查时钟漂移。尚无P95延迟合格结论。

- 032154：同路径再次在双脚接触mask=15时触发骨盆修正12.2399cm，确认接触安全失败；故障帧回滚后，交接期间最大相邻骨骼变化8.002度并最终解除过渡，未恢复旧位置。
- 新路径条件候选：先有限速度转向，随后有界加减速，末端制动；到达时长包含以上动作。测试覆盖120度/秒、0.75m/s²、最高速度、路径几何与失败不取消旧意图。最新全量377 passed, 1 skipped, 301 subtests passed（22.42秒）。主场景复测尚未完成。

- 032923：有限转向/加减速路径真实到达通过，世界2336ms完成；之后连续待机20秒、正常退出通过，实际执行548模型帧，无急停。公共UTC校验后首次新意图应用到渲染1.433秒（仅单次，不是P95）。原Life启用，ARDY独占期间Animator开启采样数0。
- 该通过仅是执行链路：转向阶段头/手最大约24.57/27.35度，交回后脚部约40.48度变化，不能宣称动作质量全通过。
- 找到交回后测速跳变：Life暂停期间冻结_motionLastPos/_motionLastYaw，恢复后将累计位移除以单帧dt。候选改为接管期间只读更新基准、丢弃过期fidget计时，并在退出混合前清零旧移动参数。正在复测。

- 033353：路径再次完成（2385ms），其后约14秒待机出现pose_lease_lost；之前20秒成功不能代表持续稳定。释放后1.5秒内骨骼最大变化8.002度，确认Life测速基准修复消除了该次恢复突变。
- 033647：自由挥手任务完成（3963ms），其后待机stream_underrun。该次控制HTTP最长22ms，端口未报信用超时；记录发送阶段、端口原因和缓冲水位，保留首个错误。
- 播放进度改为实际Applied后附带到接收ACK，无输入时独立补发，降低每秒额外5条日志压力。378 passed, 1 skipped, 301 subtests passed（15.49秒），Udon编译通过；真实复测待做。

- 0346复测发现已及时缓冲的帧因157ms渲染停顿被误判迟到。接收器改为比较实际到达时间，LateUpdate最多依次追赶4帧；仍逐帧验证控制权、碰撞和接触。Udon实际编译错误检查通过，未延长500ms授权租约。
- 0355路径到达检查失败，实际终点误差约21.3cm；发现新检查写死15cm，而主场景原导航stopDistance为30cm。当前采用世界配置并限制1–30cm，不把基础导航到达当成座椅/抓握精确对齐。
- 040254真实100次切换测试：10个意图实际开始，第11个前端口receipt_timeout，随后世界pose_lease_lost。此轮失败，未完成100次验收；事件顺序检查未发现log_seq倒序。
- 041330复测：4个意图开始后stream_underrun；端口事务ACK正常，发送链单次HTTP最大272ms。新增有界逐事务排队/首发/末发/确认时间，区分网络阻塞与端口信用超时。
- 接收证明、实际应用回执及下一块获取合并为有界exchange候选，保留分层身份校验和失败不推进本机水位；381 passed, 1 skipped, 301 subtests passed（18.15秒）。真实复测待做。

- 042139：合并交换后9个意图实际开始，仍在314ms渲染停顿后欠载；HTTP最大206ms，端口ACK最大约272ms。停顿前仅剩约一个线帧缓冲，尚不稳定。对齐截图时刻后，314ms尖峰并不在额外截图帧，不能把截图认定为根因。
- 042447：持续流水单包发送间隔缩短到250微秒（旧协议仍500微秒），998普通事件/秒、112未确认及两个急停预留不变；边界与过载测试通过。真实复测仍在首个新任务前失败：HTTP约506ms超时，世界随后失去租约；活动段最大渲染间隔约117ms，因此并非单一Unity渲染问题。
- 新增受控推理子进程，HTTP/时间线不与PyTorch共享GIL，检查点跨进程只传独立CPU数值；超时回收自有子进程，主进程结束后子进程退出，不自动复活。真实spawn健康并行/超时回收/检查点隔离测试通过。386 passed, 1 skipped, 301 subtests passed（15.70秒）；真实模型切换复测待做。

- 043301：推理进程隔离后36次实际切换，HTTP最大32ms，健康P95约13.5ms且无错误。首帧完整应用P95约1.116秒，Animator同时开启采样0；仍因上行ACK延迟与欠载失败，不能算100次通过。
- 上行仲裁：姿态/命令/持续控制回执优先，其他遥测最多6行/1.05秒、普通快照最多2行，总预算20行不变；独立播放进度改为300ms节流，发送后更新时间。隔离C#测试证实ACK越过积压进度、总预算生效且保留未发进度，Udon编译无错误。
- 043923：60次实际切换，首帧完整应用P95约1.241秒、最大1.351秒，HTTP最大35.5ms。第61次前有效控制帧已确认，但接收器仍用最后姿态包的另一时钟判过期，未接收新意图首帧；随后端口超时和pose_lease_lost。此次仍失败。
- 持续协议改为Router、接收器、骨骼执行器读取同一个500ms授权时钟；原有限姿态协议仍保留接收超时。增加生命周期共享state_revision/control_state、迟到开始不复活终态、运动取消走强制队列。388 passed, 1 skipped, 301 subtests passed（17.05秒）；Udon编译无错误。

## 最新复测与未完成项（2026-09-12 05:30）

- 044706：31次实际开始后失败，播放进度等待约491ms。独立进度恢复150ms节流并纳入回执优先队列；20行总预算与6行非回执预算不变。
- 045247：51次实际开始后欠载，HTTP最大36.5ms，姿态ACK延迟约325ms。增加有界主线程分派延迟记录；最多补偿400ms且仍逐帧检查碰撞与授权，不延长500ms授权。
- 050018：42次实际开始后失败，342ms主线程停顿与授权到期相邻。观察到Unity与推理使用同一GPU，但尚未证明GPU竞争是根因。
- 051206：关闭持续图重放的首次测试在握手阶段ACK超时，未进入动作，不作为图重放对照。
- 052514：重建Play后握手、持续待机成功；第一个新意图开始前姿态包出现约535ms发送空档，随后pose_lease_lost。HTTP最大24.5ms、活动段最大渲染间隔约100ms。该轮同时加入面部通道，不是严格单变量对照；不能得出关闭图重放可修复的结论。持续图重放恢复默认启用，保留环境开关供后续对照。
- 面部通道：NekoArdyFaceSetup从原Animator提取8种表情、18个BlendShape通道，保留动态眨眼与嘴部曲线；120Hz以上采样，四分点校验最大权重误差0.0971。仅写BlendShape，身体及退出过渡由原仲裁链执行。表情过期计时不再被身体接管提前返回跳过。实际接管100个渲染采样中面部播放器均运行；这不替代口型、表情切换和视觉质量验收。
- 主场景保存前备份磁盘和未保存状态：`H:/AI/NVidia/integrations/migration-backups/face-20260912-052058`。正式场景持续能力仍关闭，测试后退出Play并回收测试模型进程。
- 最新Python回归389 passed、1 skipped、301 subtests passed（16.43秒）；Udon编译无错误。最新摘要及原始数据见`evidence/continuous-20260912/verification-latest.json`和同目录各次运行证据。

**整份方案尚未完成。** 基础阶段仍被真实切换中的发送/进度空档和主线程停顿阻断，100次切换与30分钟持续运行均未通过；不能发布完整持续接管能力。后续需先补齐每次候选生成、废弃结果、拉取等待和提交边界的可关联时序，定位空档后再重复验收，不能单纯扩大授权期限。速度/朝向与组合动作的正式世界验证、关键帧/局部约束可达性、坐标窗口重定位、座椅/抓握/台阶/跳跃/攀爬/动态平台及活动宿主全链路仍待实现或验收。


## 2026-09-12 17:40 卡顿续走与路径窗口交付

本节覆盖此前阶段性“仍受5米限制”和“重连只恢复待机”的结论。证据位于 `evidence/stall-path-20260912/`。

- 已实现：故障后的同身份迟到终态仍被核对；只有世界确认因链路故障取消的显式到达型路径可恢复。新会话重新查询当前位置到语义目标的NavMesh路径，生成新op_id；旧帧与旧完成状态不复活。新指令、人工停止、完成、替换和世界变化撤销恢复资格；未知提交不重试。
- 173217实测通过：Unity主线程实际间隔1.280秒，故障后1.656秒完成新会话接管；旧任务取消，新任务自动续走约4.63米，世界完成回执11375ms。
- 173402实测通过：故障后人工急停，6秒内未自动重连或续走。
- 170849及173532分别完成五段连续路径；后者强制线上坐标偏移-32米，实际跨5次编码边界且全部取得完成回执。根窗口展开保持包长133字节，无额外逐帧ACK；旧v1限制保留，新v2路径取消旧会话原点5米限制，仍受NavMesh、碰撞、速度、自由动作区域及场景边界校验。
- 步幅修复：根运动尺度使用腿长比，身体碰撞尺度独立；速度使用一致的世界米/秒单位。20→10Hz接触降采样保留单帧抬脚。不能据此宣称鞋底滑动指标通过。
- 起播缓冲改400ms，200ms不可变窗口、500ms授权期限、998事件/秒和112未确认预算不变。长卡顿仍可断流，由重握手及新路径恢复；不保证任意卡顿期间动作连续。
- 失败记录保留：170435旧5米限制拒绝；171520欠载；172943首次重连成功后又欠载，未完成；172756状态诊断代码错误导致探针结果不完整，随后修正并回归。不能只用成功轮次宣称30分钟稳定性通过。
- 回归429 passed、1 skipped、301 subtests passed；最终目标回归9 passed。Udon编译无错误。退出Play后控制台另有ProTV TVManagerV2.cs:498异常，未纳入ARDY修复，不宣称场景无所有运行时错误。
- 同步5个C#源文件及20个编译资产/元数据，正式SAVE.unity与实施前备份SHA256完全一致；持续能力仍默认关闭。已退出Play、停止本轮自有模型服务。未启动VRChat，未重载活动NEKO宿主。

仍未完成：自由动作/复杂交互的进度恢复、速度/组合动作完整验收、正式关键帧与接触可达性、座椅/抓握/台阶/跳跃/攀爬/动态平台、30分钟稳定性、鞋底指标、活动宿主全链路与多人验收。后空翻模型审计只显示无XZ约束时能出现翻转，同时有重复翻转及漂移；固定XZ条件不可靠，不能发布世界空翻能力。整份动作系统方案尚未全部完成。


## 2026-09-12 全地图巡游与行走摇摆实测

- 主场景ClientSim、真实ARDY及MIDI，四轮固定路线测试；22个目标导航均可达，17个取得实际operation_completed。30次目标请求，51条目标记录（含故障后未执行），实际活动采样累计约129.22米，途中1次自动重连续走完成。
- 全地图连续巡游未通过：north_corner触发pelvis_correction_limit，需要12.15799cm骨盆下降，超过12cm；nook_rug的路径地面检测失败；cinema_aisle、cinema_front、media_lounge被平地高度限制拒绝。路径拒绝缺少明确回执，会引起端口超时。另有电视区→大厅、大厅→画廊途中unsupported_ground，说明目标能在某轮走到也不等于所有相连路线稳定。
- 摇摆已确认但未修复：923个有效移动采样，输入躯干倾角P95 18.68°/最大32.34°，最终骨骼P95 18.86°/最大32.23°，躯干横向范围−29.65°至+14.19°；同期Animator身体写入采样0。该段倾斜已存在于MIDI解码输入姿态，尚未隔离提示词、路径条件及生成模型各自影响。
- 本轮只增加测试脚本、观测和证据；未修改正式运行算法、放宽接触/地面限制或开启正式持续能力。场景SHA256与原备份一致；退出Play并停止自有测试服务，未启动VRChat。
- 原始事件、轨迹、步态输入/输出和覆盖表：`evidence/map-tour-20260912/REPORT.md`。固定路线测试不等同后台语义规划模型自主选择目标，仍不宣称“像玩家一样自行探索”或动作质量通过。
# 2026-09-12 19:00 行走与曲线转弯修复

- 已实现并单客户端验证：稀疏Root2D路径条件、0.8m/s世界速度、世界验证的外侧转弯余量及相切曲线、曲率限速与连续加减速、路径拒绝明确回执、三维地面路径元数据、接缝及地毯斜边的有效支撑复核。500ms约束候选保留，不能以直角转弯中的倾角直接否定它。
- 185716实测六段地面路线均完成：沙发、大厅、床边、照片墙、画廊、北侧角落；成功路线共13个批准曲线拐角。楼下影院仍因stream_path_constraint取消。435项回归通过；Udon编译无错误。
- 仍未通过：楼下/台阶、全地图巡游、严格同条件转弯质量对照、鞋底滑动、100次切换和30分钟稳定性。三维锚点与地面跟随不等于任意三维运动能力。其他复杂模式和活动宿主/多人验收不变。
- 主项目及Unity的3个源文件、12个编译资产/元数据一致，场景哈希与本轮备份一致，正式持续能力仍关闭。证据：`evidence/gait-surface-20260912/REPORT.md`，包含未通过的候选，不能只引用最后六段成功结果。

## 2026-09-12 楼梯改用斜面

按用户要求停止逐级台阶扩展。主场景两段楼梯已替换为无渲染的斜面碰撞，保留楼梯外观；显式绑定斜面的支撑高度补偿已同步。真实实测下到第一段近底部仍因骨盆额外修正 0.12344m > 0.12m 停止，随后上坡返回 gallery_center 完成；完整上下楼未通过。4秒后空翻任务回执完成，但生成姿态倒立帧数为0、俯仰范围约60°，不能发布后空翻能力。436项回归及301个子测试通过，持续开关默认关闭。详细改动、备份与原始证据：Docs/ARDY/evidence/stairs-ramp-backflip-20260912/REPORT.md。此节为最新范围，不把共享斜面方案记成逐级足部支撑能力。


## 2026-09-12 NavMesh 区域与软接触（覆盖上述旧约束策略）

按用户最新要求，持续 ARDY 模式不再用正常姿态的不确定性作为取消依据。以下覆盖旧的一米自由动作半径、二十厘米路径走廊、固定骨盆高度及统一根速度硬阈值；旧有限 v1 执行保持兼容。

- 新世界声明 `pose_navmesh_region_v1` 与 `pose_soft_contacts_v1`，仍由正式持续开关控制发布。运动根在当前可达 NavMesh 区域执行；路径任务仍要求目标有效并实际到达，不能把离开给定轨迹等同任务完成。
- 根位置通过当前点/目标点最近 NavMesh 采样和一次 NavMesh 射线验证。水平边缘误差不超过 5cm 时投影回网格；射线命中边界时，允许将同样不超过 5cm 的误差裁回命中点的当前可达侧。实际根不越过网格边界、空洞或断开的区域。
- 直线受阻时才额外查询局部 NavMesh 路径；完整短路径按下一拐点执行，最多6个角点且绕行增量不超过25cm。它修正小拐角，不逐帧进行全地图搜索；大范围绕行仍交给世界路径规划。
- 台阶碰撞替换为斜面后，烘焙导航高度可能与真实支撑高度不同。当前位置在1.25m内查找导航表面，下一点以该导航高度为中心在0.5m内采样；水平边界仍受5cm投影及可达性检查。导航高度不直接驱动根高度，实际地面支撑/身体碰撞保持独立。
- 足部接触失败、超出辅助纠正量或不可达时释放该脚锁定，200ms 后可重新捕获。骨盆辅助补偿有界并平滑；辅助限值不再是整个动作失败条件。不强拉骨骼，也不靠扩大 IK 力度掩盖模型姿态。
- 保留真实身体碰撞、根支撑、控制权、有效时间戳、通信租约和急停。没有安全支撑时沿用有界路径恢复与绕行；没有可行路线返回明确失败。NavMesh 地面区域不是任意空中锚点或跳越空洞能力。
- 模型 20Hz、线上 10Hz、133 字节姿态包、998 事件/秒、112 未确认事件、急停预留和 200ms 不可变窗口不变。新增检查为本地执行校验，无逐帧新 ACK、无新增主模型工具。
- 真实动作试验 225027：待机、蹲起、跳跃、后空翻均有世界完成回执；后空翻实际倒立 10 个采样，水平位移约 3 米，8 秒动作实际 7.96 秒。结束骨盆朝上分量约 0.10，未稳定站立，不能发布高质量后空翻或空中根物理能力。首段路径租约丢失也保留在结果中。
- 软接触注入 225800：足端极低辅助限值触发释放后，原画廊任务正常完成且无需恢复；整轮最大释放计数 12。下楼随后发现 NavMesh 边缘射线误判，已修正微小边界裁切，修正后的回归结果见报告。

原始执行、输入姿态、渲染采样、图像与交付核对：`Docs/ARDY/evidence/navmesh-soft-20260912/REPORT.md`。历史 100 次切换及卡顿/障碍恢复证据另见 `evidence/ramp-recovery-actions-20260912`；不等同本版所有质量指标重新验收。持续开关默认关闭，当前只使用 Unity 主场景 ClientSim，未启动 VRChat。

最终版231733真实回归：画廊→影院→画廊3/3世界完成，软接触释放计数最大31、姿态约束取消0。下楼途中一次租约丢失后自动新任务续走完成，上楼返回完成；不是零中断或完整动作质量验收。1409段历史成功轨迹的新导航校验回放无拒绝。原始记录和候选失败均见navmesh-soft-20260912/REPORT.md。

## 2026-09-12 后空翻复测与跨机运行

三次10秒后空翻尝试：一次欠载；两次世界完成并恢复直立，其中一轮出现两段翻转，另一轮一段翻转。不能据此发布可靠单次后空翻或空中根物理能力。迁移实施与原始证据见evidence/backflip-portable-20260912/REPORT.md，Windows CUDA/XPU配置、安装及验收边界见PORTABLE.md。
2026-09-13最终按用户要求改为轻量接入包（约34MB），不包含ARDY源码/模型；使用者下载官方基线并应用49项补丁。干净源码构建与新原生扩展导入通过，5项检查通过；新环境XPU Core40实际生成40帧/20Hz通过，该次生成0.388秒，缓存后加载预热201秒。轻量后台Core40Eager外部路径检查ready=true约73秒，未绑定世界且测试后关闭。CUDA硬件未测。详见PORTABLE.md及evidence/backflip-portable-20260912/REPORT.md。
