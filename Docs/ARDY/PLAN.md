# ARDY / YUI 接入执行计划

更新：2026-09-11。按用户安排先做单客户端本地测试，不上传世界。动作传输、旋转重定向、平地根位移与脚踝接触约束已进入真实 VRChat；完整动作系统尚未完成，execution_ready 继续保持 false。

## 已完成

- [x] 隔离候选插件、正式世界备份；最近核对 495 个备份原文件全部未变。
- [x] 主模型精简入口、插件端语义规划、环境去重直接注入与过期失效。
- [x] ARDY 未连接保留旧导航/动作与停止；世界未连接明确返回未连接。
- [x] 独立 ARDY 服务、Core40 优化运行时、任务/实例/会话/取消代次、有限缓冲与租约。
- [x] 压缩 Core27 MIDI 编解码、CRC、单帧回执、500ms 超时停止。
- [x] 统一端口发送线程与 112 条未确认信用上限；候选真实命令/心跳/文本接入，停止优先。
- [x] 外部姿态回执与内部发送 ticket 严格绑定；宿主生命周期挂接，丢失绑定不自动恢复同一会话。
- [x] 原版 SDK 输入实测：10 FPS 200/200 帧；混合模拟流量与一秒停顿下停止。SDK 临时修复已按用户要求恢复。
- [x] 实际编辑器 Udon VM 执行；修复负 int 转 uint 在 Udon 中抛异常的问题，代理测试不能替代此项。
- [x] Core27 到目标角色的旋转标定、X 镜像、绑定姿态与插值；独立场景 ArdyPoseLab。
- [x] SDK 本地 Build & Reload，真实 VRChat 使用 --midi=NEKO_MIDI 加载独立世界。
- [x] 客户端待机 20/20、连续重复待机 200/200、行走姿态 20/20 回执。播放画面确认角色从 T 姿态进入垂手姿态，停止后恢复。一次重载期间武装超时安全退出，重载后重新发起成功。

- [x] v2 四位足部接触标记保持 133 字节/78 个事件；16 种组合实际 Udon VM 通过。
- [x] 隔离根轨迹比例与 X 镜像标定、脚踝锁定/双骨 IK/有界骨盆补偿；速度、范围、地面和胶囊碰撞门控，拒绝已绑定的导航写入者冲突。
- [x] 新版客户端：两秒行走 20/20 帧、位移 1.061m；两段连续行走 40/40 帧、位移 1.901m；连续待机 200/200 帧。执行器记录锁定脚踝误差四舍五入为 0mm，不等于鞋底脚滑验收。未完成连续步态的视觉质量验收。
- [x] 异常大位移客户端试验：接收回执 2/2，但执行器拒绝第二帧，位移 0，原因 trajectory_speed_or_time。探针将接收状态和执行状态分开报告。

候选完整回归：306 passed、1 skipped、292 subtests passed。服务及执行桥针对性回归 36 passed（随后新增坐标转换 17 项、配置重建 1 项已纳入完整回归）。真实模型历史首段待机 1.68s / 行走 0.97s，续段约 0.18–0.19s / 40帧；样本不足以声称 P95 达标。

## 2026-09-11 完整接入推进记录

- [x] 实时 HTTP 分段 → 完整 Core27 数据校验 → 统一发送器 → 实际 VRChat 接收、应用、完成三级回执。新增 v3 结束控制包，保持 133 字节/78 事件，必须在末帧应用后才能完成。
- [x] 实际 Udon VM 验证结束包不能早于第一姿态、不能跳号、不能用接收代替应用；16 种接触标记继续通过。
- [x] 实时生成待机与行走各 40 个传输帧完成，均收到 npc.pose_completed；最新行走位移 0.870m。这是隔离世界完成，服务记录 isolated_completed，不能当作正式 npc.operation_completed。
- [x] 保留 live-walk-bounds-failure.json：前一轮在 0.757m 处触发 trajectory_bounds。重复成功没有证明随机越界已修复；新分段已完整保存，新执行器将高度/距离失败分别报告。
- [x] MotionDispatch 将受理任务交给 MotionExecution，显式任务和基础语义任务共用调度。停止打断创建竞争、关闭回收适配器；世界工厂未安装时主工具保持旧入口。正式世界工厂仍待实现与安装。
- [x] 坏首段在武装前拒绝；分段拉取与取消均匹配任务身份。单次生成违反约束仅结束该任务，运行时故障才熔断模型服务。
- [x] 修复重新配置后现有工具面仍引用旧后端的问题，诊断状态增加动作执行状态。
- [x] 新增独立 motionRoot；编辑器实际 Physics 验证父根移动 1.089m、模型子节点局部位移不变。此修改通过 Udon 编译，尚未重新打包到客户端，最新客户端实时证据对应修改前的实验包。
- [x] 世界路径坐标转换支持角色比例、朝向和 X 镜像，拒绝未支持的台阶/越界路径。只有收到世界验证路径后才可使用，当前尚未接入正式路径遥测。

最近一次实时服务加载 31.91s、预热 56.64s；本次服务已由启动它的监督进程关闭。495 个备份原文件全部未变，原插件工作区保持干净。

## 2026-09-11 控制权和正式日志适配推进

- [x] 正式 Router/Locomotion 以当前正式源码为基线新增默认关闭的姿态接管接口；保留原有修复。新增取得/续租/释放、14 种互斥身体命令拒绝、停止和控制权丢失回收。C# 实际门控及恢复测试通过；授权取得控制权的客户端正向测试尚未完成。
- [x] 新增 NekoArdyWorldBridge：v4 准备路径、v5 武装；通过现有 receiver 转发同一个 MIDI 流。只从正式 NavMesh 与已发布 anchor 获取平地路径，最多 16 个拐点；5 秒准备期、起点/朝向变化检查、500ms 接管租约、到达误差检查。新类已生成实际 Udon 编译产物，未配置进场景或实测正式 MIDI 握手。
- [x] SessionPoseWorld 正式日志适配器：验证世界、会话、操作、姿态代次和日志序号；同时要求 npc.pose_completed 与匹配的 npc.operation_completed。正常完成关闭监听时不急停共享端口；失败与旧会话切断发送。6 项针对性测试通过，运行时工厂还未安装。
- [x] 准备/武装控制包维持 133 字节和 78 个事件；6 项 Python 契约测试通过。v4/v5 尚未在真实客户端验证。
- [x] 原接收器实际 Udon VM 回归继续通过：16 种接触标记、CRC/旧包/末帧完成门控。新版候选完整回归 318 passed、1 skipped、292 subtests。

本阶段开始有意更新共享运行时：495 项备份中，变动仅为 NekoMidiRouter.cs/.asset 和 NekoNpcLocomotion.cs/.asset，其余 491 项未变。SAVE 场景未变，SDK 两个原文件校验通过。变更前源码备份和部署校验在 unity-backups/20260911-205158-root-lease；编译资产原件在此前 175237 备份中。旧“495 项全部未变”的记录属于此前阶段。

下一步仍需实现并安装宿主路径准备工厂、服务正式世界绑定及 capability 握手，然后配置独立的完整 NPC 测试场景进行本地验收。当前没有新增客户端完成证据，不开启正式家园姿态能力。

工厂接入时必须区分服务任务 epoch 与世界 pose_epoch；不能沿用旧 PoseLifecycle 把每次任务代次变化当作断线，也不能让旧生命周期与新工厂各自接管同一个 MIDI sink。正式适配器已有完成后保留共享端口的行为，但工厂尚未负责整个准备/武装与端口生命周期。

## 下一阶段：完整动作执行

### 动作与对话的关系（用户确认）

后台动作模型持续负责动作生成、填补与过渡，动作时间线独立推进。字幕只展示回复，不触发、不阻塞、不裁剪身体动作。后续将说话/倾听状态、对话意图及情绪作为后台动作模型的上下文输入，在可衔接边界更新；明确任务和确定性路径/碰撞约束保持优先。环境和对话状态去重注入，不增加主模型工具。

不得将字幕出现或消失当作 TTS 播放开始/结束；当前只读回复源没有真实语音播放回执。仍需接通后台连续填补、状态影响与实测，不能把离散动作执行通过视为这一部分已完成。未部署的 caption 参数及“字幕提交后才武装”候选逻辑已撤回。共享 MIDI 的分段文字进度确认独立保留，不与动作时序绑定。

### 2026-09-11 宿主准备与完整测试场景接入

- 已实现 SessionPoseFactory 并安装到候选插件生命周期：单个共享发送器完成 v4 路径准备、服务绑定、v5 武装、分段回执。独立保存服务 epoch 与世界 pose_epoch；移除候选插件对旧 PoseLifecycle 的实例化，断开先释放工厂再关闭传输。
- 服务新增 neko 绑定模式，任务沿用世界准备的 op_id；正式成功需要同世界、同会话、同任务、同姿态代次、正确末帧序号的 pose_completed 与 operation_completed。缺少或错误终态不得消费末段缓冲；isolated 仍只产生 isolated_completed。
- Router 动态握手只在执行桥现场引用校验完整时发布 pose_stream_v1/pose_operation_v1（bit 21/22）。Udon 编译已通过；正式 SAVE 的默认关闭状态保持不变。
- 正式模式最初合并接收与应用回执，通过完整链路后发现每帧等待插值造成串行延迟；修订 10 改为即时接收 ACK，最后插值完成由双终态确认，不增加逐帧应用日志。
- 新建 ArdyWorldBridgeLab 独立场景，包含完整 Router/Sync/Locomotion、真实 NavMesh、原角色与三个锚点。Build & Reload 已成功加载至单个 VRChat 客户端。
- **离线客户端验证通过**：完全禁用 ARDY 时，forward/home 两次导航均有真实 operation_completed；当前客户端已发布两项姿态能力。证据：unity_lab/world-bridge-offline-01/result.json 与 events.json。
- Core40 实时生成到完整世界桥已通过：world-bridge-live-05-retry-2/result.json 中原地及一米步行均有 40 帧、finish 41 和双终态，服务 succeeded，共享端口未确认事件为 0。修订前耗时分别 9.417/9.377 秒。
- **修订 10 客户端通过**：world-bridge-overlap-01-retry-1/result.json 中原地 4.994 秒、步行 5.007 秒，较修订前缩短约 47%；两者均得到双终态和服务 succeeded，最终控制状态 external，端口 outstanding=0。这些是两段样本，不是 P95 或自然动作质量验收，仍比标称 4 秒慢约 25%。
- 首次重叠传输测试因操作终态在普通遥测队列排队约 680ms 超时，保留 world-bridge-overlap-01 失败证据。只在真实 pose_completed 已收到后，给 operation_completed 独立最多 1.5 秒等待；500ms 接收/执行租约不变，不重发姿态。新增延迟日志测试通过。
- 回归：完整 364 passed、1 skipped、292 subtests；随后终态等待修改及新增用例的相关 20 项测试全部通过。实验接收器已 Udon 编译且无错误，当前仍只有一个原 VRChat 客户端。临时服务由监督进程关闭，收尾急停保持，不自动恢复执行。
- 以上不代表正式 SAVE、活动 NEKO 宿主或多人验证完成。正式 SAVE 哈希仍为 CBBBEB02940D90ED6D9713645506A64568BC0C1899BF2E779B3279E92CE7169A；构建临时 lilToon 设置已自动恢复，495 项备份仍只存在四项预期共享运行时变更。重叠传输修改前实验接收器备份：unity-backups/20260911-receipt-overlap。
- 原有 495 文件核对仍只有 Router/Locomotion 的 .cs/.asset 四项预期变更；SAVE 和其他 491 项未变。增量备份：unity-backups/20260911-capability-binding。

- [x] 将真实世界状态、路径和导航约束送入服务，接通生成 chunk 到 PoseReceipts 的生产桥；已在完整协议的独立场景验证。
- [ ] 将已验证的根位移执行器接入正式 NavMeshAgent 路径/控制权交接与恢复；目前仅实验中拒绝已绑定的活动写入者。
- [ ] 鞋底脚滑实测、脚趾滚动、摆动脚碰撞、台阶及动态平台；当前仅平地胶囊碰撞与脚踝接触锁定。
- [ ] 正式控制权与 capability 握手、长时间时间戳规则、欠载回退及最终 operation_completed。
- [ ] 活动 NEKO 宿主验证工具注册/断后端/断世界/重连；当前仅候选及 SDK 隔离验证。
- [ ] 单客户端完整链路：提示词→语义规划→连续生成→走到目标→世界完成回执。实时 ARDY→隔离世界已完成；语义规划、活动宿主与正式控制权仍未串联。
- [ ] 30 分钟连续运行与 CPU/GPU/帧时间、欠载、停止延迟测量。
- [ ] 坐下/起身、物体交互、台阶、跳跃、攀爬分别实现并验证。
- [ ] 双客户端与晚加入：按用户当前只有一个客户端的安排延后。

验收目标仍为有意义响应 P95 < 500ms、脚滑 <= 3cm、到达误差 <= 20cm；这些不是当前结果。收到姿态 ACK 只证明接收，不等于动作完成。当前不部署到正式家园、不发布生产姿态能力。

## 主模型工具

| 状态 | 工具 | 行为 |
|---|---|---|
| 世界就绪、ARDY 未就绪 | npc.execute_plan、npc.stop | 原导航/动作 |
| 服务和世界姿态执行全部就绪 | npc.perform、npc.stop | 连续动作；当前门控不满足 |
| 世界未连接 | npc.stop | 明确返回未连接 |

基础语义动作扩展现有 AutonomyIntentProvider，环境直接注入上下文，不增加主模型规划/环境查询工具。

证据：unity_lab/client-idle-result.json、client-idle-preview-result.json、client-walk-result.json、client-walk-reload-interrupted.json、udon-vm-result.json、original-files-verification.json。客户端探针为 client_probe.py，仅允许隔离场景显式武装，不用于生产控制权认证。

根位移证据：unity_lab/ground-result.json、ground-first-failure.json、client-ground-walk-result.json、client-ground-continuous-result.json、client-ground-idle-result.json、client-ground-rejection-result.json。首次行走可达性失败被保留；修正没有放宽停止阈值，而是区分踝/趾接触并在 12cm 内补偿骨盆高度。


## Unity 沙盒与视觉问题修复（2026-09-11）

后续测试按用户要求使用 Unity 内置隔离沙盒，不启动 VRChat。此前协议成功不代表动作自然度通过：用户观察到本地测试 NPC 扭曲和平移，视觉验收明确未通过。

- 控制权恢复闭环已在主项目代码验证：integrations/acceptance/recovery-live-01-retry-1 中，ARDY 待机、行走后，同一会话三次原导航均 arrived。首次首段生成超时记录保留在 recovery-live-01；尚不能由此宣称欠载恢复完成。
- 新增 NekoArdySetup 编辑器接线工具，预检骨骼/导航、备份场景、默认关闭、重复接线拒绝，并检查原有 Transform 姿势未变。实际安装仅在 ArdySetupValidation 副本完成。正式 SAVE 未改。
- 确认旧重定向使用接管瞬间动画姿势作为基准，会重复叠加姿势。现从蒙皮绑定矩阵提取固定 bindRotations，另存进入前姿势用于恢复；缺少固定基准时不发布执行能力。修复已同步主项目和 Unity 工程，并更新两个隔离测试场景的绑定数据。
- Unity 实际 C# 姿态对照：同一帧由绑定姿势/窄站姿接管，旧算法最大差异约89.98度，修复后约0.000016度；退出恢复误差0度。证据与前后图片在 evidence/unity-retarget。
- Physics 回放约一米根位移通过。脚踝 IK 误差不是鞋底脚滑指标；鞋底、脚趾和动态自然度尚未验收。obstacle_ahead 旧探针把墙放在固定+Z，当前样本朝-Z运动，未命中不能算障碍通过。
- NekoArdySandbox 提供 NEKO/ARDY/动作回放沙盒菜单，支持保存素材实时预览及旧算法对照；临时角色与地板不保存到场景，停止或关窗清理。素材在 integrations/acceptance/sandbox-fixtures。该路径直接回放 C# 与 Physics，不等于 ClientSim/Udon VM 或真实客户端验收。


## 主场景迁移（2026-09-11）

按用户要求，执行桥、姿态接收器与固定绑定基准的骨骼执行器已接到 Assets/SAVE.unity 的 NekoNpc_YUI/ARDYExecution，并保存。正式场景现已发生本次明确授权的接线变更；此前“正式 SAVE 未改”属于历史阶段。1033 个 Renderer 与488个 Collider 配置前后相同，安装工具核对原有 Transform 姿势未变。备份及校验在 H:/AI/NVidia/integrations/unity-backups/20260911-main-scene-transfer。

姿态接管保持关闭：自动审批拒绝额外启用，理由是本轮用户只授权迁移，需明确启用授权。未上传世界，未启动 VRChat，主场景运行时效果尚未验收。


### 主场景接管已获授权启用

用户明确回复“对，启动”后，SAVE 中 enablePoseStream 已开启并保存，桥接引用检查 Ready=true，Udon 编译已执行。启用前备份：H:/AI/NVidia/integrations/unity-backups/20260911-main-enable。已请求 Unity Play Mode；不启动 VRChat。接管能力开启不代表已收到生成帧，主场景动作效果仍需实际执行验证。


## 主场景完整验收推进（2026-09-12）

总体验收仍未通过，详见 [ClientSim验收记录](history/CLIENTSIM_ACCEPTANCE.md)。本次真实主场景22/22锚点到达；ARDY未连接的两分钟自主复测失败0；空间拒绝后的有界驻足回退已在主项目实现并通过真实MIDI验证；主场景ARDY原地动作收到双终态和服务成功，标称4秒实际5988ms。完整回归369 passed、1 skipped、301 subtests passed。

原导航部分到达误差约34–37cm，尚不满足严格20cm条件。后台模型填补与活动宿主全链路、ARDY复杂地形、鞋底脚滑及动态自然度、断后端平滑恢复、30分钟性能与稳定性仍未验收。双客户端继续按此前安排延后。不能由本次部分通过宣称NPC动作系统完美。


## ARDY 比例、传输及完成回执修复（2026-09-12）

详见 [本轮修复记录](history/ARDY_FIXES_20260912.md)。固定绑定位置后，默认与窄站姿步行位移一致；主场景真实待机、行走双终态通过。4秒待机实际4607ms（前次5988ms），行走4297ms。每秒MIDI总预算和未确认信用不变；motion操作完成回执进入优先队列。完整回归373 passed、1 skipped、301 subtests passed。

整体自然动作验收仍未完成：尤其是鞋底脚滑、动态视觉质量、正常停止的姿态衔接、连续动作填补及复杂地形。不能把本轮性能和比例修复等同于动作完美。

### 2026-09-12 实际动作画面验收

动作质量未通过。原配置发现头颈重复写入、结束姿势硬恢复、旧 action 状态覆盖造成接续任务拒绝；关闭注视并增加任务间隔的诊断对照可完成待机和约 43cm 行走，但不能替代原配置验收。证据及边界见 [动作验证报告](history/MOTION_VISUAL_VALIDATION_20260912.md)。本轮没有保存主场景设置或修改运行时代码。
